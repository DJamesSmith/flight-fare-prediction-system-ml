Step 1.
For mac/Linux:
python3 -m venv flight_fare_env

Activate it:
source flight_fare_env/bin/activate

For Windows:
flight_fare_env\Scripts\activate

Step 2.
Then install the required libraries
Start with:
pip install pandas matplotlib scikit-learn reportlab psycopg2

Note: Pandas automatically installs NumPy dependency, because numpy is dependent on pandas.

Later, if you decide to use XGBoost:
pip install xgboost

Generate requirements.txt (Once everything is installed):
pip freeze > requirements.txt

Your requirements.txt will look something like:
numpy==...
pandas==...
matplotlib==...
scikit-learn==...

You don't need to type versions manually—pip freeze will generate them.

FlightFarePrediction/
│
├── flight_fare_env/
│
├── dataset/
├── models/
├── services/
├── validation/
├── utilities/
├── controllers/
├── views/
├── reports/
├── trained_models/
│
├── index.py
│
└── requirements.txt